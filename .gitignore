# OS 관련
.DS_Store
Thumbs.db
desktop.ini

# Python 캐시
__pycache__/
*.py[cod]
*$py.class

# IDE 관련
.idea/
.vscode/
*.swp
*.swo

# 환경 변수
.env
.env.*
!.env.example

# envs 폴더 - 민감 파일만 무시
envs/.local.env
envs/.prod.env

# 프로젝트 설정 (공통)
.claude/
.ruff_cache/
.pytest_cache/
.amazonq/
PLAN.md

# 로그 및 임시 파일
*.log
logs/
logs.prev/
nul

# 식약처 sync raw 백업 / DB dump
ai_worker/data/medicines_*.ndjson
ai_worker/data/medicines_*.json
medicine_data*.sql
medicine_data*.sql.gz

# 보안 관련
*.pem

# 로컬 전용 문서 (개인/민감 정보)
docs-private/

# 디자인 참고용 캡처(재생성 가능, 저장소 미포함)
design-refs/
docs/plans/plan_recall_tool.md
# 세준작성 사용

# SQL문서
*.sql
*.pyc

# Coverage (수집 산출물 — scripts/coverage_contexts.py collect)
.coverage
.coverage.*
coverage.xml
# 🔴 `coverage.json` 은 산출물이라 무시하고, `.coverage-baseline.json` 은
#    게이트의 기준이라 **커밋한다**(하이픈이라 위 `.coverage.*` 에 안 걸린다).
coverage.json
htmlcov/
